[Unit]
Description=MHFS server
After=network.target

[Service]
Type=simple
User=AAA
WorkingDirectory=/home/AAA/MHFS
ExecStart=/bin/bash -i -c 'exec /usr/bin/perl server.pl &>> /var/log/mhfs.log'
Restart=on-failure

[Install]
WantedBy=multi-user.target